/* Generated by TestGen on Tue Sep 21 2021 at 7:31 AM (GMT) */
@isTest
private class OpportunityApprovalTrigger_TGN_TEST{
    @isTest
    private static void run(){
        Database.SaveResult dsr;
        Opportunity drOpportunity;
        drOpportunity=new Opportunity(StageName='a',CloseDate=Date.today());
        dsr=Database.insert(drOpportunity,false);
        Database.update(drOpportunity,false);
    }
}